<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Vibration for haptic feedback. -->
    <uses-permission android:name="android.permission.VIBRATE" />
    <!-- Microphone for voice typing (requested at runtime via VoicePermissionActivity). -->
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <!-- Network: GIF search + in-app update checks. -->
    <uses-permission android:name="android.permission.INTERNET" />
    <!-- Lets the in-app updater launch the package installer (user must still confirm). -->
    <uses-permission android:name="android.permission.REQUEST_INSTALL_PACKAGES" />

    <!-- Allows querying whether a speech-recognition service exists on the device. -->
    <queries>
        <intent>
            <action android:name="android.speech.RecognitionService" />
        </intent>
    </queries>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.PearlKeyboard">

        <!-- The keyboard itself. Must be guarded by BIND_INPUT_METHOD and exported. -->
        <service
            android:name=".ime.IosKeyboardService"
            android:label="@string/app_name"
            android:permission="android.permission.BIND_INPUT_METHOD"
            android:exported="true">
            <intent-filter>
                <action android:name="android.view.InputMethod" />
            </intent-filter>
            <meta-data
                android:name="android.view.im"
                android:resource="@xml/method" />
        </service>

        <!-- Settings / "open app" launcher screen. -->
        <activity
            android:name=".settings.SettingsActivity"
            android:exported="true"
            android:label="@string/settings_title"
            android:theme="@style/Theme.PearlKeyboard">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Theme customizer (background image, blur, accent, …). -->
        <activity
            android:name=".settings.ThemeActivity"
            android:exported="false"
            android:label="@string/pref_theme_customize_title"
            android:theme="@style/Theme.PearlKeyboard" />

        <!-- Transparent helper that requests the microphone permission for the IME. -->
        <activity
            android:name=".feature.voice.VoicePermissionActivity"
            android:exported="false"
            android:excludeFromRecents="true"
            android:theme="@style/Theme.Transparent" />

        <!-- Serves the downloaded update APK to the package installer. -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>
    </application>
</manifest>
