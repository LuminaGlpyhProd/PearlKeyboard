<?xml version="1.0" encoding="utf-8"?>
<resources xmlns:tools="http://schemas.android.com/tools">

    <!-- App UI theme (settings screen). Material 3 DayNight follows the system,
         giving us automatic light/dark for the host app. -->
    <style name="Theme.PearlKeyboard" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="colorPrimary">@color/brand</item>
        <item name="android:colorBackground">@color/app_bg</item>
        <item name="android:statusBarColor">@color/app_bg</item>
        <item name="android:windowLightStatusBar" tools:targetApi="m">true</item>
        <!-- Edge-to-edge friendly. -->
        <item name="android:windowDrawsSystemBarBackgrounds">true</item>
        <!-- Required by the AndroidX Preference library. -->
        <item name="preferenceTheme">@style/PreferenceThemeOverlay</item>
    </style>

    <!-- Invisible activity used only to prompt for the microphone permission. -->
    <style name="Theme.Transparent" parent="Theme.Material3.DayNight">
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowContentOverlay">@null</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowIsFloating">true</item>
        <item name="android:backgroundDimEnabled">false</item>
        <item name="android:windowAnimationStyle">@null</item>
    </style>
</resources>
